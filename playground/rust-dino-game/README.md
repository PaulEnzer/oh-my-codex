# Rust Dino Game

Bundled local GUI dino game used by OMX team-play-window smoke flows.

## Run

```bash
cd playground/rust-dino-game
cargo run
```

## Notes
- Native GUI game (macroquad), not a terminal-rendered app.
- OMX team mode may launch this as a separate external window for visual play.
